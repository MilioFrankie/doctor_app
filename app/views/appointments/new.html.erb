<h1> Add Appointment</h1>
<%= form_with(model: [@doctor, @appointment], local: true, class: "ui form") do |f|%>
  <%= f.label :date %>
  <%= f.text_field :date, required: true, placeholder: "month/date/year"%>
  <%= f.submit nil, class: "ui green button" %>
  <%end%>